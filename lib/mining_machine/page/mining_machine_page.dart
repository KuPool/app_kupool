import 'package:flutter/material.dart';

class MiningMachinePage extends StatelessWidget {
  const MiningMachinePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('矿机'),
      ),
    );
  }
}
